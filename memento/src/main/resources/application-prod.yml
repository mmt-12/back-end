spring:
  datasource:
    url: jdbc:mysql://memento-mysql-prod:3306/memento
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none

  data:
    redis:
      host: memento-redis-prod
      port: 6379